{
  "name": "rhinozug",
  "version": "1.0.0",
  "description": "Rhinogram's internal database connection module",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -Rf lib && babel src -d lib --copy-files"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "knex": "^0.11.7",
    "mysql": "^2.11.1",
    "postgresql": "0.0.1"
  }
}
